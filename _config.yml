# Site settings
title: "Kathryn Shaw Artist"
description: "Contemporary Canadian Artist"
url: "https://www.katyshaw.ca"
baseurl: ""

# Collections
collections:
  projects:
    output: false
    sort_by: year

# Defaults
defaults:
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "project"

# Build settings
markdown: kramdown

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .sass-cache
  - .jekyll-cache
  - .gitignore
